# Geometry identifiability

The retained Herschel/ALMA products are diagnostic map/visibility or Laplace outputs, not a fully sampled joint posterior with validated correlated-noise and injection-recovery coverage. The robust result is a broad belt with an outer scale near 50--60 AU. Inner edge and position angle remain model-sensitive and are not promoted to validated primary intervals. No formal narrow-ring exclusion is claimed.
